const SkillBadge = ({ children }) => (
  <span className="rounded bg-blue-100 px-2 py-0.5 text-[11px] font-medium text-blue-800 shadow-sm sm:text-xs dark:bg-blue-900 dark:text-blue-200">
    {children}
  </span>
);

export default SkillBadge;
